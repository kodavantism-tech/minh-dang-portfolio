/**
 * Canonical origin. Change this in one place if the site moves to a custom
 * domain — metadata, robots.txt and the sitemap all read from here.
 */
export const SITE_URL = 'https://dangquangminh.vercel.app';
